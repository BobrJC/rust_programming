struct Rect {
    height: u32,
    width: u32,
}

fn main() {
    
    let first_rect = Rect(
        height: 10,
        width: 10,
    );

    let second_rect = Rect(
        height: 20,
        width: 30,
    );

    for i in 
}
